import { ShadowCard } from './shadow-card';

describe('ShadowCard', () => {
  it('should create an instance', () => {
    const directive = new ShadowCard();
    expect(directive).toBeTruthy();
  });
});
